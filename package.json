{
  "name": "hemitech-site",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "HemiTech marketing site, the media hosting API that Buffer fetches from, and the generator that assembles the static pages.",
  "scripts": {
    "vercel-build": "echo \"Pre-built site: HTML is committed, and .vercelignore strips build/ and scripts/ from the deployment. Nothing to build here.\"",
    "build": "node scripts/build-site.js",
    "check": "node scripts/build-site.js --check",
    "shots": "node scripts/capture-shots.js",
    "og": "node scripts/build-og.js",
    "dev:media": "node scripts/dev-server.js",
    "test:media": "bash scripts/acceptance.sh",
    "test:site": "node scripts/acceptance-site.js"
  },
  "dependencies": {
    "@vercel/blob": "^0.27.0"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "jsdom": "^30.0.1",
    "sharp": "^0.35.4"
  }
}
